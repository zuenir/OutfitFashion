import React from 'react'

import { Box } from '../../Components';

interface LogoutProps{}

const Logout = () => {
    return (
        <Box flex={1} backgroundColor="drawer1"/>
    );
};

export default Logout;
